
#include <stdio.h>

#include "miniminied.h"

int main_loop() {
    
    // get line

    while( true ) {
        switch(c) {
            case P:
                break;
            default :
                break;
        }
    }
    return 0;
}
